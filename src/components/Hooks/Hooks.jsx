import { Button } from "@mui/material";
import { useState } from "react";

export default function Test() {
  console.log(useTate(0));
  return (
    <div>
      <Button variant="contained" color="secondary">
        Click Me!!!
      </Button>
    </div>
  );
}
